#Id Type Sinks   Key Interpreter Task
0   M    1,3     -   awk         inline:'{ print $ENVIRON["BRED_PART_ID"],i++,$0; }'
1   R    2       1   awk         inline:'BEGIN { i=0; } { i++; } END { print ENVIRON["BRED_PART_ID"],i; }'
2   L    >offset -   awk         inline:'BEGIN { i=0; } { print $1,i; i+=$2; }'
3   M    4       1   awk         inline:'BEGIN { read offset from a file here; offset=0; } { l=offset+$2; $1=$2=""; print l,$0; }'
